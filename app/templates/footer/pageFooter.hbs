{{#if meta}}
<aside class="lo-full padding-off">
  <ul class="page-footer-view list list-inline">
    {{#each meta}}
    {{#if url}}
    <li><a href="{{url}}" class="btn btn-primary btn-line">{{name}}</a></li>
    {{else}}
    <li>{{name}}</li>
    {{/if}}
    {{/each}}
  </ul>
</aside>
{{/if}}
